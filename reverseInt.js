var reverse = function(x) {
    const flip = function(x){
      const str = x.toString()
      const arr = str.split('')
      const flippedArr = []
      for (let i = arr.length - 1 i >= 0; i--){
        flippedArr.push(arr[i])
      }
    }
    if (x === 0) {
      return 0
    }
    if (x < 0) {
  
    }
  };
  
  reverse(123)
  reverse(-123)